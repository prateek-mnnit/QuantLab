# Used by the Prisma CLI (migrate, studio, generate) when run from the repo
# root. Matches the credentials in docker-compose.yml for local development.
DATABASE_URL="postgresql://quantlab:quantlab_dev_password@localhost:5432/quantlab?schema=public"
